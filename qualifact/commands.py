## List of user defined commands preceded by a slash symbol (/) due to the planned usage of ephemeral commands  
def calc_cv(cr, cd):
    return (2 * cr) + cd


def calc_bv(cr, cd):
    return 1 + cr * cd

