# File of commands and functions

# Calculate crit value (CV) as well as build value/quality
# CV = (CR * 2) + CD, BV/Q = (1 + CR) * CD, courtesy of OkCode, Xlice

def parse_image():
    return

def calc_cv(cr, cd):
    return (cr * 2) + cd

def calc_bv(cr, cd):
    return (1 + cr) * cd